# Dio — next-session seed

Read this once, then proceed.

## Project in 30 seconds

Multiplayer arcade racer in Unity 6 + URP, networking via **Mirror** (vendored at `Assets/Mirror/`, gitignored). Tracks are geodesic strips around a small planet. Car physics: server-authoritative WheelCollider + custom spherical gravity. UI built programmatically by `Tools → Dio → Build → All`.

The full design + audit lives in [plan.md](./plan.md). Recent run-through and the pain-point list is below.

## MCPs available in this session

Two HTTP MCP servers were registered in user-scope (`~/.claude.json`) and confirmed connecting:

| Name | URL | Purpose |
|---|---|---|
| `unity-mcp` | `http://127.0.0.1:8080/mcp` | Driving the running Unity Editor (scene inspection, GameObject queries, console logs). Provided by `com.coplaydev.unity-mcp` package — Unity must be open with the project loaded for this to respond. |
| `mirror-docs` | `https://mirror-networking.gitbook.io/docs/~gitbook/mcp` | Querying Mirror Networking docs (NetworkTransform, NetworkRigidbody, Discovery, etc.) |

If your tool list doesn't show them at session start, run `/mcp` once — Claude Code hot-loads them on demand.

If `unity-mcp` is failing to connect, ensure Unity Editor is running with the project open (the MCP server lives inside the Editor). The mirror-docs MCP works from anywhere with internet.

## Project conventions you must respect

- **Repo hygiene**: `.gitignore` excludes `Assets/Mirror/`, `Assets/Plugins/`, `Assets/Dio/Prefabs/`, `Assets/Dio/Scenes/Main.unity`, `Packages/manifest.json`, `Packages/packages-lock.json`, `Packages/*/`, `*.meta`, and Unity's auto-generated URP / autosave files. **Never commit any of those.** `git add -A` is risky — review with `git status --short` first.
- **Generated content** (prefabs, the Main scene, default level, default vehicle profile, default skybox material) is regenerated by `Tools → Dio → Build → All`. Each clone runs that menu after pulling.
- **No `.meta` files in git**, on purpose. Cross-clone GUIDs differ; everything that depends on prefab GUIDs is regenerated by editor menus.
- The user's GitHub account is `noelalemwork-bit`. Repo: `noelalemwork-bit/dio` (private). Default branch `main`. The `gh` CLI is auth'd.

## Tech stack pinned

- Unity 6 (URP, Input System, ProBuilder, Vector Graphics, TextMeshPro)
- Mirror Networking (vendored under `Assets/Mirror/`); KCP transport; `Mirror.Discovery.NetworkDiscoveryBase` extended into `DioNetworkDiscovery`
- Optional: `com.unity.vectorgraphics` for vector-quality SVG rendering (we fall back to plain `Image` if missing)
- `com.coplaydev.unity-mcp` is in `Packages/manifest.json`

## What's done (don't redo)

The full audit is in [plan.md §11](./plan.md). Quick summary of fixed items:

- Wheels touch ground; visuals spin/steer locally on every peer (driven by `transform` velocity + `DioCar.steerAngle` SyncVar)
- Remote rb config is strict: `kinematic + None + Discrete`
- LAN discovery enumerates every up IPv4 adapter and broadcasts to each subnet's specific broadcast (works around WARP / VPN)
- Direct-IP fallback in browser panel; IP cached in `LocalPrefs.LastDirectIp`; Enter triggers Connect
- Powerup row group constraint (`PowerupBox.groupId` + `PowerupHolder.consumedGroups` SyncList) — one consume per pass
- Win condition: server polls car distance to `level.Finish`; sends `RaceWonMessage`; locks all inputs; `ServerCleanupRace` destroys per-race objects after 5.5s; clients restore lobby
- Win UI: `winner_banner.svg` + `UIConfetti` (UI-Image-based, no extra camera)
- Minimap is a 256×256 RT from `MinimapRenderer`; ortho 130; UI markers projected via `WorldToViewportPoint` with hemisphere cull; panel 330 px
- Procedural shaders: `Dio/CarBodyCamo`, `Dio/CarWheel`, `Dio/EarthPlanet`, `Dio/MysteryBox`, `Dio/SunsetSky`. All use `_DioTime` (NetworkTime) + `_PlayerUp` globals so visuals are consistent across peers
- Vehicle parametrised by `VehicleProfile` ScriptableObject (mesh + dimensions + suspension)
- Track strip mesh from `TrackBuilder.Build(level)`; LineRenderer removed; planet + track on Layer 8 (`MinimapLayer`)

## Open / unverified pain points

These are the ones the user wants you to investigate with the MCPs:

1. **Joined-player physics still felt frozen on their screen** in earlier tests. Strict kinematic config in `DioCar.OnStartClient`, plus the new World-coords + every-tick streaming setup on `NetworkTransformReliable` (configured by `Dio.Net.EditorTools.NetworkTransformConfigurer`), should fix this. To verify under suspicion:
   - `mirror-docs`: "For a kinematic client receiving server-authoritative NetworkTransform updates, what Rigidbody settings does Mirror recommend? Is NetworkRigidbody preferred?"
   - `unity-mcp`: inspect a running joined client's `DioCar` GameObject during a race — confirm `Rigidbody.isKinematic == true`, `NetworkIdentity.connectionToClient` matches, `transform.position` is changing.
2. **LAN auto-discovery over Cloudflare WARP** — multi-adapter broadcast in `DioNetworkDiscovery.MultiAdapterBroadcast`. Verify `[Dio Discovery] server got ping from …` shows in the host's console when a client clicks *Join Game*.
3. **Predicted rigidbody for the local player** (input-lag fix) — Mirror's `PredictedRigidbody` lives at `Assets/Mirror/Components/PredictedRigidbody/`. Open question whether it composes with `WheelCollider` (Mirror's prediction skips `Physics.Simulate()`). M3.

## What changed in this session (latest)

### Level editor overhaul — yOffset + world rendering

- **TrackPoint.yOffset** (`LevelData.cs`): new `float yOffset` field for per-anchor height above planet surface (non-negative).
- **Shift-drag arrows** (`LevelEditorWindow.cs`): holding Shift shows yellow tangent arrows at every anchor; dragging an anchor in this mode edits `yOffset` via ray-to-line intersection. Normal anchor drag is unchanged; track extension moved to Ctrl+click on endpoints.
- **TrackBuilder yOffset propagation** (`TrackBuilder.cs`): `Build`, `BuildGuards`, `BuildSpawnPad`, `SampleAt`, and `ArcLengthOf` all linearly interpolate `yOffset` across each bezier segment so the lifted track, guard rails, and spawn pad follow the authored offsets.
- **World.fbx rendering fix** (`LevelEditorWindow.cs`): PreviewRenderUtility can't reliably bind URP shaders, so both fallback and planet materials now force built-in `Unlit/Color`. All submeshes of the FBX are drawn. The planet sphere is drawn 0.5 % inset so the FBX surface sits cleanly on top without z-fighting.

### Powerups — Blue Shell leader targeting

- `BlueShellObstacle.FindLeaderCar()` now uses `DioCar.progressArc` (server SyncVar, fed by `TrackBuilder.ArcLengthOf`) instead of the placeholder `Vector3.Dot(position, forward)` metric.

### Code hygiene

- `CarStateFactory` "kind enum hack" comments replaced with accurate description of the intentional multi-kind → single-state mapping.

### Networking — switched to client-authority physics

The car was server-authoritative (Cmd → server sim → NetworkTransform back), which added a full RTT of input lag and, in practice, caused joined-player cars to refuse to accelerate. Per the project goal of "perfect physics, no latency on LAN," the car is now **client-authoritative**:

- **Local owner runs full physics** (rb.isKinematic = false). They feel zero input lag.
- **`NetworkTransformReliable.syncDirection = ClientToServer`** on the Car prefab — the owner pushes position+rotation up; the server relays to other clients. Cf. [Mirror docs on SyncDirection](https://mirror-networking.gitbook.io/docs/manual/general/syncdirection).
- **Server + remote clients** keep cars kinematic; transforms are driven by NetworkTransform OnDeserialize. Triggers (powerup boxes, win detection) still fire because the kinematic body has a Rigidbody.
- **Powerup state machine** input modification moved from `DioCar.FixedUpdate` (server-only) to the owner's path. `ArcadeCarController.readLocalInput = false` always; DioCar.FixedUpdate is the single writer of `currentInputs`.

### Other tuning

- **NetworkTransform target field set explicitly** on every prefab (reflection-built prefabs serialize with target=null; OnValidate sets it in editor but not from build scripts). Fixed in [NetworkTransformConfigurer.cs](Assets/Dio/Scripts/Net/Editor/NetworkTransformConfigurer.cs).
- **World-coords, every-tick streaming, 5 mm precision on cars** / 1 cm on obstacles. `onlySyncOnChange = false` for the car so restart-stutter is impossible.
- **60 Hz everywhere**: `Time.fixedDeltaTime = 1/60`, `NetworkManager.sendRate = 60`. Pinned in `DioNetworkManager.Awake`.
- **Race start delay = 4 s** to host the cinematic intro + 3-2-1-GO countdown.

### Visual / collision pass

- **Higher-poly planet** ([HighResIcoSphere.cs](Assets/Dio/Scripts/Level/HighResIcoSphere.cs)): subdivision-4 icosphere, ~2.5k verts. The track ribbon now visibly aligns with the surface.
- **Side-guard rails** ([TrackBuilder.BuildGuards](Assets/Dio/Scripts/Level/TrackBuilder.cs)): both edges of the track get a thin wall mesh (height 2.5m, skirt 3m below surface). Inward-facing winding so cars on track see the wall, off-track sees only backside.
- **Bouncy chassis PhysicMaterial** on the body collider so car-to-car body collisions feel Mario-Kart-like instead of phasing.
- **Circular minimap mask** — `Mask` + procedural disc sprite ([Assets/Dio/UI/Generated/MinimapCircle.png](Assets/Dio/UI/Generated/MinimapCircle.png)). Markers are siblings of the RawImage under the mask, so they render on top.

### Race feel

- **Cinematic intro** ([RaceIntro.cs](Assets/Dio/Scripts/UI/RaceIntro.cs)): camera starts high above the player, swoops down via a slow chase-lerp, while a centered TMP text bounces "3 ... 2 ... 1 ... GO!". Pop animation: scale 0.4→1.25→0.95→1.05→1.0 with overshoot, alpha holds till 70% then fades. Inputs are locked locally on every car until GO!.
- **Arc-length tracker**: `TrackBuilder.ArcLengthOf(level, worldPos, planetCenter)` for accurate lap/finish detection. `DioCar.progressArc` SyncVar feeds the HUD position panel.
- **Ping HUD**: `RaceHUD.pingLabel` shows live `NetworkTime.rtt` with green/amber/red colour bands.

## How to use the MCPs efficiently

- **`mirror-docs`** — fact-check Mirror's APIs and recommended patterns *before* writing/changing networked code. Especially for `NetworkTransform`, `NetworkRigidbody`, `NetworkDiscovery`, `[Command]` authority rules.
- **`unity-mcp`** — read scene state, inspector values, console logs while Unity is running. Don't use it to write/edit assets that the user hasn't asked you to touch — the editor menus are the canonical write path.

## First-message kit

When you first take this session, check the MCP tools are loaded:

```
ToolSearch query="mirror" max_results=10
ToolSearch query="unity scene" max_results=10
```

If they're not present, run `/mcp` and try again.

## Run instructions for the user

```text
Window → TextMeshPro → Import TMP Essential Resources    # first time only
Tools → Dio → Build → All                                # every time scripts change
File → Open Scene → Assets/Dio/Scenes/Main.unity
Press Play → name + color → Host Game → Start Race
Controls: WASD / arrows, Space brake, Shift handbrake, Tab cycle camera, E activate powerup
```

## Editing rules

- **Don't write `.meta` files into the repo.** They're gitignored on purpose.
- **Don't reintroduce `Library/PackageCache/` paths in shaders.** Use `UnityCG.cginc` (legacy) for skybox + procedural shaders so they work on both URP and Built-in.
- **Don't break the editor-menu rebuild flow.** Anything generated should be re-runnable by `Tools → Dio → Build → All` without hand-editing prefabs.
- **Always check `git status --short` before `git add -A`.** Several Unity auto-generated assets at `Assets/`-root and `Assets/Resources/` will sneak in otherwise.

## Repo

- **Origin**: `noelalemwork-bit/dio` (private)
- **Default branch**: `main`
- **gh CLI**: authenticated as `noelalemwork-bit`

That's everything. Read [plan.md](./plan.md) for the full design + audit table.
